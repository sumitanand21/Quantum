import { MultiValueFilterPipe } from './multi-value-filter.pipe';
import { DataCommunicationService } from '@app/core/services/global.service';
describe('MultiValueFilterPipe', () => {
  // it('create an instance', () => {
  //   const pipe = new MultiValueFilterPipe(new DataCommunicationService);
  //   expect(pipe).toBeTruthy();
  // });
});
